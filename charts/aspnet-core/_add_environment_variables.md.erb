<%
=begin
apps: aspnet-core
platforms: kubernetes
id: add_environment_variables
title: Add extra environment variables
category: configuration
weight: 50
=end %>

In case you want to add extra environment variables, use the *extraEnvVars* property.
~~~
aspnet:
  extraEnvVars:
    - name: LOG_LEVEL
      value: error
~~~

Alternatively, use a ConfigMap or a Secret with the environment variables. To do so, use the *extraEnvVarsCM* or the *extraEnvVarsSecret* values.
