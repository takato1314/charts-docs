<%
=begin
apps: kafka
platforms: kubernetes
id: scale_down
title: Scale down the cluster
category: administration
weight: 40
highlight: 40
=end %>

To scale down the Kafka cluster just perform a normal upgrade setting the *replicaCount* value to the desired number of nodes. For example, reduce the cluster to the default (one node):

~~~
$ helm upgrade bitnami/kafka \
--set replicaCount=1 \
~~~
