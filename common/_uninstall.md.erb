<%
=begin
apps: all
platforms: kubernetes, tanzu-application-catalog
id: uninstall
title: Uninstall the chart
category: get-started
weight: 20
highlight: 20
=end %>

To uninstall your chart deployment, run the command below. Replace the MY-RELEASE with your chart name. This command removes all the Kubernetes components associated with the chart and deletes the release.

    $ helm delete MY-RELEASE

The command removes all the Kubernetes components associated with the chart and deletes the release.

    $ helm delete MY-RELEASE
