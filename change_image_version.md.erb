To modify the application version used in this chart you can specify a [valid image tag](https://hub.docker.com/u/bitnami) using the *image.tag* parameter. For example, *image.tag=X.Y.Z*. This approach is also applicable to other images like exporters.
